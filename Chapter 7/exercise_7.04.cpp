//Exeercise 7.4: Write a class named Person that represents the name and address of a person. 
// Use a string to hold each of these elements. Subsequent exercises will incrementally add features to this class
#include <iostream>
#include <string>

struct Person {
	std::string Name;
	std::string Address;
};

int main {
	return 0;
}